aws_ses_monitor:
    aws_config:
        credentials_service_name: 'test'
    mailers:
        - default
    bounces:
        topic:
            endpoint:
                host: localhost.local
        filter:
            enabled: true
    complaints:
        topic:
            endpoint:
                host: localhost.local
        filter:
            enabled: true
