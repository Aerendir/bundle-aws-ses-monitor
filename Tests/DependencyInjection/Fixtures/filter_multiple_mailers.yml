aws_ses_monitor:
    aws_config:
        region: us-east-1
    mailers:
        - default
        - second
        - third
    bounces:
        endpoint:
            host: localhost.local
    complaints:
        endpoint:
            host: localhost.local
