aws_ses_monitor:
    aws_config:
        region: us-east-1
    mailers:
        - default
        - second
        - third
    bounces:
        topic:
            endpoint:
                host: localhost.local
    complaints:
        topic:
            endpoint:
                host: localhost.local
