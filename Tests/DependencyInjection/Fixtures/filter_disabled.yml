aws_ses_monitor:
    db_driver:            orm
    model_manager_name:   null
    aws_config:
        region:               us-east-1
    bounce_endpoint:
        route_name:           _shivasbouncebundle_bounce_endpoint
        protocol:             HTTP
        host:                 localhost.local
    filter:
        enabled:              false
        filter_not_permanent: true
        mailer_name:
            - default