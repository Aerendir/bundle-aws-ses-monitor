aws_ses_monitor:
    aws_config:
        region: us-east-1
    mailers:
        - default
    bounces:
        topic:
            endpoint:
                host: localhost.local
        filter:
            enabled: false
    complaints:
        topic:
            endpoint:
                host: localhost.local
        filter:
            enabled: true
